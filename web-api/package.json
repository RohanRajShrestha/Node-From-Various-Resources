{
  "name": "web-api",
  "version": "1.0.0",
  "description": "Architecture we will be following\r N - Layers\r database\r     - we will be using json file for that\r src (to compile all the source code)\r     - entitites (object mapping)\r     - factories (popular for creating instances instance generators)\r     - repositores (data access)\r     - routes (endpoint mappings)\r     - services (communications between the routes and repositories layer (business logix))\r     - util (shared code)\r     - handler (communication between routes and server)\r     - index.js (server instance)\r tests (all automated test suites)\r     - integreation tests - testing on the user point of view, it's also E2E test because there's no app consuming it\r     - unit test all test that must run without any externam connections such as databases, externaml APIs and on our case, the fileSystem",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "node --test tests/",
    "start" : "node src/index.js",
    "dev" : "nodemon src/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
